{
  "name": "pith",
  "private": true,
  "version": "0.0.0",
  "description": "Pith — Web Components with three visual personalities",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r --parallel exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
